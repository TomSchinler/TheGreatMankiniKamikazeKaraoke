<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tomschinler.thegreatman_kiniskamikazekaraoke" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_mankini"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".KamikazeSelect"
            android:label="@string/app_name"
            android:parentActivityName=".TitleActivity">
        </activity>
        <activity
            android:name=".SongActivity"
            android:label="@string/title_activity_song"
            android:parentActivityName=".KamikazeSelect" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="tomschinler.thegreatman_kiniskamikazekaraoke.KamikazeSelect" />
        </activity>
        <activity
            android:name=".TitleActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
